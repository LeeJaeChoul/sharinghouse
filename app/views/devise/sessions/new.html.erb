<div class="container" style="margin-top: 200px; border:1px solid #eee">
<h2 class="text-center">로 그 인</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="input-group">
	  <span class="input-group-addon" style="padding-right: 30px;">이메일</span>
	  <%= f.email_field :email, autofocus: true, class:"form-control", placeholder: "dongguk@dgu.edu" %>
	</div><br />
  <div class="input-group">
	  <span class="input-group-addon">비밀번호</span>
	  <%= f.password_field :password, autofocus: true, class:"form-control" %>
	</div><br />
  <div class="row text-center">
    <div class="actions col-md-6">
      <%= f.submit "로그인", class: "btn btn-xl" %>
    </div>
    <div class="g-mb-20 col-md-6">
      <%= link_to "회원가입", new_registration_path(resource_name), class: "btn btn-xl" %>
    </div>
  </div><br />
<% end %>


</div>